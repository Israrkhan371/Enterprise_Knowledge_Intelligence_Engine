fastapi==0.115.0
uvicorn[standard]==0.30.6
click>=8.1.3,<8.2.0
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.3

# Ingestion / parsing
unstructured[all-docs]==0.15.14
pytesseract==0.3.13
pypdf==4.3.1
# pdfminer.six must stay at whatever version pdfplumber (an unstructured
# dependency) hard-pins — currently 20260107. Downgrading it here breaks
# the install (ResolutionImpossible against pdfplumber). The resulting
# PSSyntaxError ImportError from unstructured's PDF partitioner is fixed
# in code instead — see the compatibility shim at the top of
# app/ingestion/loaders.py.
pdfminer.six==20260107

# Embeddings / RAG orchestration
# Pinned exactly to the combination proven to install cleanly in the first
# successful build — without these exact pins, pip backtracks through 90+
# versions of llama-index-indices-managed-llama-cloud, llama-index-readers-*,
# huggingface-hub, transformers, pandas, and google-genai trying to satisfy
# loose ranges + the pdfminer.six/unstructured pins above at the same time.
llama-index==0.11.22
llama-index-core==0.11.23
llama-index-indices-managed-llama-cloud==0.6.0
llama-index-readers-llama-parse==0.3.0
llama-index-readers-file==0.2.2
llama-parse==0.6.21
llama-cloud-services==0.6.21
llama-cloud==0.1.19
huggingface-hub==0.36.2
transformers==4.46.3
sentence-transformers==3.4.1
tokenizers==0.20.3
chromadb==0.5.23
pandas==2.3.3
opencv-python==4.11.0.86

# Knowledge graph
neo4j>=5.25.0,<6.0.0
spacy>=3.7.2,<3.9.0

# Search / ranking
rank-bm25==0.2.2

# Eval / monitoring
mlflow>=2.16.2,<2.17.0
prometheus-client==0.21.0

# LLM provider (free tier — Google Gemini)
# Pinned exactly — pip backtracked from 1.75.0 down through 70+ releases
# to reach this one while also satisfying the pins above.
google-genai==1.2.0

# Utilities
python-multipart==0.0.12
httpx==0.27.2
tenacity>=8.2.3,<9.0.0

# Dev / testing only — not needed at runtime, but installed in the image
# so `docker exec ekie-api pytest ...` works without a separate dev image.
pytest==8.3.4
