fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.3

# Ingestion / parsing
# Pins loosened here on purpose: unstructured, llama-index, and pypdf each
# depend on specific (and differing) pypdf ranges. Letting pip pick a
# mutually-compatible version avoids repeated ResolutionImpossible errors.
unstructured[all-docs]>=0.15.13,<0.16.0
pytesseract==0.3.13
pypdf>=4.0.1,<5.0.0

# Embeddings / RAG orchestration
llama-index>=0.11.20,<0.12.0
sentence-transformers>=3.1.1,<4.0.0
chromadb>=0.5.15,<0.6.0

# Knowledge graph
neo4j>=5.25.0,<6.0.0
spacy>=3.7.2,<3.9.0

# Search / ranking
rank-bm25==0.2.2

# Eval / monitoring
mlflow>=2.16.2,<2.17.0
prometheus-client==0.21.0

# LLM provider (free tier — Google Gemini)
google-genai>=1.2.0,<2.0.0

# Utilities
python-multipart==0.0.12
httpx==0.27.2
tenacity>=8.2.3,<9.0.0
